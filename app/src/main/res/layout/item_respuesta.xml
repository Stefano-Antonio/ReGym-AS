<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="#f2f2f2"
    android:layout_marginBottom="8dp">

    <!-- TextView para el nombre del usuario (opcional) -->
    <TextView
        android:id="@+id/tvNombreUsuarioRespuesta"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nombre del Usuario"
        android:textStyle="bold"
        android:textSize="14sp"
        android:textColor="#000000"
        android:paddingBottom="4dp" />

    <!-- TextView para el contenido del comentario -->

    <!-- LinearLayout horizontal para el botón de eliminar al lado del EditText -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="start|center_vertical"
        android:paddingTop="4dp">

        <TextView
            android:id="@+id/tvRespuesta"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:padding="6dp"
            android:text="Este es una respuesta de ejemplo."
            android:textColor="#333333"
            android:textSize="16sp" />

        <!-- Botón de eliminar (ahora alineado a la derecha) -->
        <ImageButton
            android:id="@+id/btn_Eliminar_Respuesta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="14dp"
            android:layout_gravity="end"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="0dp"
            android:src="@drawable/borrar" />

    </LinearLayout>


</LinearLayout>
